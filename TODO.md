# TODO

* Upscale Images & Video using SeedVR2 One-step diffusion DiT + 3D-causal VAE
* Built in Transcriber
* Prompt to Lyrics helper
* Expand Chat Tools to be able to generate media
* Stretch: DSpark/EAGLE3-class trained drafter (ARahim3/mlx-dspark) — needs a per-target trained 5-layer backbone (none exists for Qwen3.6-27B); 
* M5 Nax
* P2P
* Launch code via cli
* DSV4 serial-perf round DONE (2026-08-01, uncommitted): decode **22.86 → ~26.6 tok/s (+16%)**, prefill 8K **~133 → ~255-270 tok/s (~2x)**, greedy output byte-identical throughout, test_dsv4.sh 14/14 + template 17/17 + all DSV4_MINI gates on the final tree. Shipped (each kill-switched): (1) wo_a served quantized via batched qmm (`WO_QMM=0`; +6.3% decode, prefill-neutral, −2.9 GB RAM); (2) GPU window emission + length-only host cache via `compressorPushLight` (`GPU_EMIT=0`); (3) MoE prefill global-sort gather (the mlx-lm `_gather_sort` pattern — THE 2x); (4) hc-head sigmoid mix on GPU; (5) lazy pipelined decode — GPU embed table + lazy logits ride generate.zig's pipelined next(), pending ring-pushes drain at window boundaries (`LAZY_DECODE=0`; +6%); (6) `dsv4_sinkhorn_y` + `dsv4_hc_post` fused kernels (hc chain 14→6 ops/sublayer; `SINKY=0`/`HCPOST=0`; +2%). TRIED AND FAILED/NEUTRAL: PREFILL_SUB 1024 (wash — prefill is NOT per-expert-M-bound; env `MLX_SERVE_DSV4_PREFILL_SUB` stays); the "compressor barrier" theory of prefill (removing all 41 barriers moved nothing — the cost was the unsorted expert re-read, and after sorting, prefill is ~ALL attention-side: skip-MoE probe ≈ full-model prefill).
* DSV4 AR-kernel round DONE (2026-08-01, uncommitted): serial decode **~26.9 → ~29.6 tok/s (+10%)**, test_dsv4.sh 14/14 + all 17 DSV4_MINI gates + kill-switch arms green on the final tree; DSpark healthy (31-48 content-dependent, acceptance 1.8-3.4). Shipped default-ON: `dsv4_emit_win` fused boundary emission (+1.5%, `EMIT_KERNEL=0`), `dsv4_dec_chain` fused per-head RMS/rope/sim chains (**+8.6%**, `DEC_CHAIN=0`), boundary drain prefetch (bit-identical wash, `DRAIN_PREFETCH=0`). Measured AGAINST, shipped opt-in: fused MoE gate+up on 2-bit banks (−2.5% vs stock gather, `MOE_GATEUP=1`), fused sink-softmax (neutral + byte-changing, `SINK_SOFTMAX=1`). Greedy bytes moved (exact-sim + reduction-tree drift, sanctioned); 6-prompt characterization equivalent. Full flash-style MQA sink-attention kernel DE-PRIORITIZED on the sink-softmax evidence (GEMMs efficient, glue overlapped). Probes: `MLX_SERVE_DSV4_LAYER_CAP=N` / `MLX_SERVE_DSV4_SKIP_MOE=1` (timing-only, garbage output).
* DSV4 comp_in int8 requant DONE (2026-08-01, uncommitted): **+7-8% serial (29.4 → 31.6 tok/s)**, DSpark+flag 44-47 tok/s. Ships EXPLICIT-opt-in on `--decode-attn-quant` (`decodeAttnQuantExplicit` — CLI/app-toggle/env=1, never the silent default): the 7-prompt characterization found one duplicated paragraph on the baseline-degenerate B-tree prompt (dense arm clean), so answers moved and dsv4 doesn't ride the flag's default. Side copies int8-g32, 362 MB, C ≤ 32 serving (decode + verify + tiny suffixes), big prefills dense, GPU-only. Remaining parked lossy lever: bf16 KV/slot GpuRows mirrors (~0.6 ms decode; real payoff is prefill gather traffic — bigger surgery, snapshot/anchor/consumer dtype sweep). Prefill still owes the msv_attn-class hd-512 gathered-K attention (big job).
* DSV4 + pi agent VALIDATED (2026-08-01, two full runs, ctx 40960, RAM peak 124.3/128 GB, ≥2.2 GB free, zero trouble lines): tool calls, DSML parsing, multi-round tasks all clean. Finding: **DSpark is greedy-only BY DESIGN** (accept = raw argmax equality; `greedy_clean` in generate.zig requires temp<0.01, no penalties/grammar/logprobs) and the checkpoint ships generation_config temp 0.6 — so agent CLIs that omit temperature (pi does) run SERIAL (~27 tok/s w/ requant). Deployment recipe: boot with `--temp 0` → DSpark engages on real agent traffic at 3.4-4.5 accepts/round, decode 47-60 tok/s (~2x). Future lever: stochastic DSpark acceptance (the MTP `probsAllPositions` pattern) so default-sampling agent traffic gets the speedup without pinning temp. App toggle loose end CLOSED: `ServerOptions.decodeAttnQuantChoice` is tri-state (nil = untouched → no flag → dsv4 dense; explicit true/false → `--decode-attn-quant`/`--no-…`), with a decode migration that treats legacy stored TRUE as undecided (synthesized encode wrote it for everyone — it must not become a silent opt-in) and preserves legacy FALSE. Guards: `testDecodeAttnQuantTriStateEmitsExplicitChoicesOnly`, `testLegacyDecodeAttnQuantBlobMigratesWithoutInventingAnOptIn`.
* DSV4 quality: on the sum-of-squares task both our arms answer `sum(range(1, 11))**2` (wrong) where antirez's imatrix-calibrated IQ2XXS GGUF answers correctly. Identical in serial and DSpark, so it is our quant mix (2-bit gate/up, 3-bit down, no calibration), not the engine. Worth trying calibrated/imatrix-style quantization for the mirror.
* DSV4 back-pocket: w2 3→4-bit requant (+5.8 GB, MLX's 3-bit kernels are its slowest). Only pays once decode is kernel-bound, and it must clear the wired-limit envelope WITH the DSpark stages (114.4 + 5.8 = 120.2 GB against 124 GB, tight). Needs re-downloading source shards; converter `EXPERT_BITS` change, w2 groups re-convert only.
* DSV4 loose ends: no prefill side-by-side vs the ds4 GGUF engine yet (needs a sequential boot, never concurrent, double-load OOMs); `docs/reference.md` still has no deepseek_v4 section; retiring `lib/ds4` is a call to make once parity is proven.
* CLAUDE.md root is 118.8 KB, over the ~100 KB growth-policy target. Needs a curation pass.
* `zig build test` reports a "failed command" on FIRST runs and is 100% green on a direct re-run. Suspected Metal contention between parallel test binaries under the build runner. Benign, but worth pinning down.
* MageFlow 4-bit. 8-bit SHIPPED (`MfLinear` + `tests/convert_mageflow_weights.py` + the two `ddalcu/Mage-Flow-*-MLX-Serve-8bit` mirrors, 17.5GB → 9.2/9.7GB, judged visually equivalent on txt2img/edit/multi-ref). `MfLinear` already solves (bits, group_size) per tensor from geometry, so 4-bit is a converter flag + validation, no engine work. Watch the AdaLN modulation projections (`img_mod.1`/`txt_mod.1`, 33% of the DiT, on the timestep path): at 8-bit they cost ~half the txt2img fixture error and `--keep-bf16 img_mod.1,txt_mod.1` buys e2e 0.990 → 0.995 for +1.27GB (not worth it at 8-bit; may be at 4-bit). Also unquantized and worth ~0.4GB: the TE embedding table, which needs a gather-then-dequantize path (it is read with `mlx_take_axis`, not a matmul).
* 